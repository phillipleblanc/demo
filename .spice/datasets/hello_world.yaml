# .spice/datasets/hello_world.yml
name: "@phillipleblanc".demo.hello_world
type: append
acceleration: {}
migrations:
  - name: create_table
    sql: |
      CREATE TABLE IF NOT EXISTS "@phillipleblanc".demo.hello_world (
        block_number BIGINT PRIMARY KEY,
        greeting TEXT
      )
