# .spice/datasets/hello_world.yml
name: '"@phillipleblanc".demo.hello_world2'
type: append
migrations:
  - name: create_table
    sql: |
      CREATE TABLE IF NOT EXISTS "@phillipleblanc".demo.hello_world2 (
        block_number BIGINT PRIMARY KEY,
        greeting TEXT
      )
